[package]
name = "rspaced-agent"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "rspaced boot agent — runs inside the RHCOS initramfs (state check, find disk, rspacefs setup, composefs-flow root mount, transition). Milestone 1: hello world to the console."

[[bin]]
name = "rspaced-agent"
path = "src/main.rs"

[dependencies]
# std-only for the hello-world milestone. Built static (x86_64-unknown-linux-musl)
# for dropping into the initramfs.
